{
  "name": "stage-london",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "STAGE. — London theatre, for less. A daily price-comparison aggregator across seven seller sources.",
  "scripts": {
    "dev": "node scripts/copy-data.mjs && vite",
    "build": "node scripts/copy-data.mjs && vite build",
    "preview": "vite preview --port 4173",
    "copy-data": "node scripts/copy-data.mjs"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.3.4",
    "vite": "^5.4.11"
  }
}
